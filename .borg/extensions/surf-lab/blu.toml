description = "Persistent deterministic surf simulation, learned-controller probes, and counterfactual trajectory analysis."
enabled = true
id = "surf-lab"
manifest_version = 1
name = "Surf Lab"
skill_roots = ["skills"]
version = "0.3.18"

[mcp.lab]
command = "${extension_dir}/surf-lab"
allowed_tools = ["start", "step", "observe", "trace", "log", "branch", "compare", "native.inspect", "native.sweep", "native.audit", "replay.audit", "replay.residual", "policy.train", "policy.train-native", "policy.inspect", "policy.compare", "policy.compare-native", "policy.track", "policy.correct", "policy.evolve", "policy.evolve-native", "map.generate", "map.inspect", "map.preview", "map.route", "map.export", "config"]
