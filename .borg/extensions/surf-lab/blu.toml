description = "Persistent deterministic surf simulation, learned-controller probes, and counterfactual trajectory analysis."
enabled = true
id = "surf-lab"
manifest_version = 1
name = "Surf Lab"
skill_roots = ["skills"]
version = "0.3.0"

[mcp.lab]
command = "${extension_dir}/surf-lab"
allowed_tools = ["start", "step", "observe", "trace", "log", "branch", "compare", "replay.audit", "policy.train", "policy.inspect", "policy.compare", "policy.evolve", "map.generate", "map.inspect", "map.preview", "map.route", "map.export", "config"]
